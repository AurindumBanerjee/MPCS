# MPCS optional dependencies
#
# The engine (Model/experimental/core/) and the web dashboard
# (Model/experimental/dashboard_web/MPCS_Test.py) need NOTHING from this file
# — they use only the Python standard library. Install these only for the
# optional dashboards and the legacy Bloom baseline.
#
#     pip install -r requirements.txt
#
# Under Anaconda, activate the environment first:
#
#     conda activate TogAI
#     pip install -r requirements.txt

# Dashboard B — Streamlit  (dashboard_streamlit/)
streamlit>=1.30
# Streamlit's st.dataframe requires pandas. numpy is pinned alongside it
# because pandas' compiled extensions are built against a specific numpy
# ABI; a mismatch surfaces as "No module named pandas._libs.pandas_parser".
pandas>=2.0
numpy>=1.26,<3

# Dashboard C — Dash / Plotly  (dashboard_plotly/)
dash>=2.14
plotly>=5.18

# Legacy baseline only — Model/reference/BloomMPCS.py
# The active engine uses its own d-left counting Bloom filter and does not
# need this.
bloom-filter>=1.3
